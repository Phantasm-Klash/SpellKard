# SpellKard Development Progress

Status date: 2026-06-28

| Area | Status | Notes |
| --- | --- | --- |
| Repository initialization | Done | README, licenses, docs, ignore rules, and directory plan created. |
| Godot project | Started | Godot 4 project and main scene committed. |
| Movement prototype | Started | Arrow/WASD movement, focus speed, and fixed local tick implemented. |
| Hitbox and graze | Started | Focus hitbox display, graze radius, hit radius, counters implemented. |
| Shooting and power | Started | Prototype player shots, focused shot shape, target dummy, and power growth implemented. |
| Bomb and invulnerability | Started | Manual Bomb, bullet clearing radius, invulnerability, and deathbomb window implemented locally. |
| Scoring | Started | Survival score, graze score, combo, multiplier, pickup-line reward, and hit penalty implemented. |
| Bullet patterns | Started | Deterministic math helpers and config-driven pattern library now cover the local Boss matrix: n-way, aimed shots, rotating rings, spirals, curtain/wall lanes, laser warning+fire, split/branching carriers, delayed blossoms, homing, orbital/lattice releases, stream/weave lanes, seeded random patterns, Boss phase scripts, and spellcard timeout/enrage rules. `BossPatternCatalog` now owns executable requirement rows, license/provenance-safe open-source recipe/adapter metadata, deterministic output validation, seed variation, shaped laser/continuous-graze metadata, behavior-spawn coverage, spellbook preview/export reproducibility, compact golden preview digests, Pattern Lab catalog/phase coverage checks, phase-local bullet-cap checks, and catalog performance-budget checks. |
| SpellCard deck prototype | Started | Local deck-builder model, prototype card catalog, rarity/type filters, server inventory level projection, server card-upgrade response projection, upgrade-cost preview metadata, UI row add/remove editing, dirty edit status, 20-card validation, invalid-save rejection, ownership/copy/high-rarity/strong-interference/ranked-ban checks, active deck save/load, active deck injection into practice, four-card hand, energy, cooldowns, self modifiers, pattern modifiers, shield, draw/energy effects, and card replay events implemented locally. |
| Chest and inventory | Started | Open-source local chest model, wallet/chest ownership, weighted pool rows, probability display, 10/60 pity counters, inventory grants through deck builder, duplicate-to-dust conversion, result rows, and opening audit log implemented. It now also applies Gensoulkyo server chest snapshots and server-authoritative open responses into wallet/chest/pool/pity/result/audit rows while refreshing deck inventory from the server response. Steam Inventory/market integration is intentionally outside this layer. |
| Replay | Started | Tick-indexed local input recorder, JSON snapshot save/load with deck_snapshot, local replay index metadata, replay-list row model, HUD replay index selection/loading/favorite/remove, snapshot playback, pause/resume, speed steps, restart, guarded target tick seek, card/hit/Bomb events, event stream, periodic state hash validation, final result hash validation, and Boss spellbook catalog/spellbook/phase preview-export metadata in practice replays implemented. Replay index rows now validate spellbook preview export id, compact preview digest, per-sample emit counts, and non-authoritative local-practice status so preview metadata cannot masquerade as server-settled results. Full visual replay list screen and indexed/keyframe seek optimization are not implemented yet. |
| Performance metrics | Started | Runtime frame time, logic tick time, bullet counts, collision checks, dropped frame count, spawn limit events, and HUD summary implemented. |
| Balance simulation | Started | Rule-level deterministic balance simulation model and headless check implemented. It reuses pattern configs, active deck cards, character configs, and bullet visual readability metadata to report average score/graze/hits/card plays, peak bullets, score rows, and blocking/non-blocking warnings across seed suites. Full server-side match simulation and real match-event imports are pending. |
| Latency matrix | Started | Local multiplayer latency matrix model and headless check implemented for 30/80/150/250ms profiles, packet loss, jitter, short reconnect, input-delay bounds, snapshot correction metrics, perceived hit mismatch, battle royale deadline stability, Boss transfer idempotency, and certification server-result display. The matrix runs against isolated model instances so reports do not mutate the active client queue/match/mode state. Real network emulation, WSS transport capture, and replay report export are pending. |
| Practice mode | Started | Stage-select model with four local practice stages, difficulty/tempo/recommended-character metadata, localized stage/pattern rows, stage briefing rows with math-route/density/danger/readability/recommended self-mode summaries, one-click Stage Practice Plan application for recommended self-mode plus first-pattern Stage Run, Stage Run automatic phase sequencing across a stage's pattern list, Boss spellbook practice run rows, pattern selection, quick restart, locked seed controls, start tick prewarm, starting power, and starting Bomb count controls implemented. |
| Pattern Lab | Started | Read-only pattern analysis model and practice/settings rows implemented for math basis, math family, parameter summaries, spawn-rate estimates, density/danger bands, readability hints, full current-pattern coverage, row selection that switches the active practice pattern, and Boss spellbook catalog/phase coverage rows with timeout/enrage, bullet cap, seed, deterministic preview signature/digest, preview export id, and preview budget headroom. |
| Characters and shot types | Started | Local balanced, precision, wide-range, and spell-power character configs implemented. Selected character now drives practice movement speed, focus speed, normal/focus shot lanes, shot cadence, shot color, Bomb radius/invulnerability multipliers, graze score modifier, spell-power gain modifier, hitbox visual scale, settings rows, and localized names. Server-validated character snapshots for online play are pending. |
| Bullet visual language | Started | Presentation-only bullet visual model implemented for small orb, large orb, star, homing, and laser-warning classifications; speed/danger hints, presentation radius safety, high-density decorative alpha reduction, card-modified outlines, homing tails, settings rows, and smoke coverage are in place. Production sprites/VFX and final theme art are pending. |
| Accessibility | Started | Low-flash mode, simplified background, bullet opacity, always-show hitbox, practice graze ring toggle, colorblind-friendly bullet palette, per-option/default reset helpers, one-click accessibility reset, and local settings persistence implemented. |
| Input profiles | Started | Required action validation, local left-hand/right-hand keyboard presets, single-action keyboard rebinding, capture-style key binding in the temporary settings shell, binding row export, restore-to-profile behavior that applies to Godot InputMap, default reset helpers for profile/binding/gamepad values, one-click gamepad reset, local settings persistence, gamepad deadzone/sensitivity/movement-curve mapping, gamepad movement-curve preview samples, actual move/focus speed preview samples for the selected character, and visible preview text in the input settings row/buttons implemented. Device-specific profile persistence and final rebinding scene UX are pending. |
| Audio settings | Started | Local settings model for master/music/SFX/UI/voice volume groups, key-event visual cues, high-frequency graze audio suppression, runtime Godot audio bus volume/mute application, per-group/default reset helpers, one-click audio reset, and local settings persistence implemented. Licensed production audio import is pending. |
| I18n | Started | en/zh-CN JSON text packages, runtime locale cycling, optional locale/theme overlay loading path, HUD key formatting, card name keys, load-error tracking, and missing-key smoke coverage implemented. Steam Workshop package download integration is pending. |
| Theme registry | Started | Runtime base theme manifest loading, local Workshop-style theme discovery, theme activation, theme text overlay loading, replacement-scope validation/rejection, HUD exposure, and localization theme-id handoff implemented. Steam API integration and theme activation UI are pending. |
| Matchmaking and network state | Started | Local anonymous dev session bootstrap, config-version status, profile/wallet/deck/activity pull placeholders, config-driven certification/PvP-duel/battle-royale/world-boss/instance-boss rows with mode category, matchmaking kind, summaries, ruleset/reward/card-pool ids, and min/max player counts, active deck validation before queueing, ranked network-quality gate, Quick/Ranked/PvP/Boss/Room matchmaking intent rows, queue/found/ready/cancel/rematch states, room-code display/pending-entry state, heartbeat presence rows, 30-second reconnect status, Gensoulkyo HTTP contract request/response projection, server inventory/card-upgrade/deck/chest/certification bootstrap and sync into deck/chest/matchmaking/mode models, certification `rating_code` loadout params, battle server allocation/signed-ticket and battle-result submit status projection, NetworkSecurityModel rows for business HTTPS/WSS/ECC/sign targets, battle ECDHE/KCP/protobuf/ChaCha20 targets, and C++ result-to-Nakama/Go callback status, business secure-envelope scaffold metadata on authenticated Gensoulkyo requests (seq/timestamp/nonce/op_code/body_hash/auth_tag placeholder plus `X-PhK-Business-*` headers including timestamp for HTTP fallback replay guard, real X25519/AEAD/sign pending), `BattleNetworkClientModel` scaffold for signed-ticket intake, descriptor-gated X25519/HKDF handshake target, KCP/UDP connection state, protobuf phk.v1 codec readiness, ChaCha20-Poly1305 pending state, and seq/ack/nonce input packet headers, Godot `HTTPRequest` transport wrappers for login/bootstrap/inventory/card-upgrade/decks/deck-save/chests/chest-open/heartbeat/queue/room/ticket/ready/battle-allocation/battle-ticket/battle-result-submit/events/mode-action/disconnect/reconnect/settlement/rematch/replay-read, and prototype network-screen actions for those flows implemented. Nakama WSS business transport, secure envelope, KCP battle transport, protobuf/AEAD client module, and polished online flow are pending. |
| Shared protocol integration | Started | `D:\gotouhou\PhK-Protocol` now contains the first shared protobuf/ruleset/schema draft for the Nakama business + C++ battle split plus `descriptors/phk_v1_descriptor.json`. SpellKard still uses the Gensoulkyo HTTP adapter and local dictionaries, but now records the draft battle allocation and signed battle ticket shape from Gensoulkyo into matchmaking/network models, loads the shared descriptor for minimal contract validation, aligns local deck/replay/test fixture defaults to the shared `ruleset-local-s0` value, and exposes the intended business/battle security split in the Network Match page. Generated protobuf binding consumption, secure-envelope client code, real KCP transport, and production AEAD are pending; the current battle client checks cover migration-time packet/header contracts only. |
| Network match authority surface | Started | Local online-match model for loading/ready/running/ended states, allowed input packet encoding, tick/seq/card-slot validation, adaptive 2-4 tick input delay, server heartbeat presence intake, server snapshot intake, authoritative server bullet spawn/sync/move/despawn projection, server active-card projection, cursor-based server event polling, server-validated mode-action event intake, recent server event logs, smooth/interpolate/resimulate/hard-snap correction classification, full-snapshot requests, mode-state summaries, battle allocation/ticket endpoint/player-id summaries, battle-result accepted/rejected/idempotency/hash/replay/key-id audit projection, battle client handshake/connection/input-header action rows for the KCP/protobuf/AEAD migration scaffold, anti-cheat forbidden-result guards, latency metrics, reconnect full-snapshot request, authoritative replay metadata, server replay audit-record intake, post-settlement rematch waiting/found/new-match loading projection, Gensoulkyo input/snapshot/events/mode-action/reconnect/settlement/rematch/replay/battle-ticket/battle-result contract projection, Godot HTTP input/snapshot/events/mode-action/disconnect/reconnect/settlement/rematch/replay/battle-ticket/battle-result wrapper coverage, server match-end settlement handoff into the result service, and presentation-only drawing of server-projected bullets during running online matches implemented. Real Nakama WSS transport, KCP battle transport, and final online gameplay screens are pending. |
| Game modes | Started | Local mode-state model for certification rating/rank/top-30% display, Gensoulkyo server certification profile/result projection, battle royale 5-10 player readiness, shared pool validation, 30-second round candidates and UI select-round-card requests, zero-round order/effect trigger display, world/instance Boss 4-8 party positions, UI transfer-card requests with duplicate rejection, server-shaped mode-action confirmation projection for battle royale and Boss transfers, world Boss persistent HP/defeat notice display, and instance Boss clear/star display implemented. Real server-side mode handlers, damage/rank/reward calculation, and polished mode screens are pending. |
| Results, rewards, and activity | Started | Local server-shaped result model for match settlements, reward ledger rows, wallet/result screen display, activity screen rows, task progress, event points, leaderboard rows, compensation claims, non-authoritative task/event/leaderboard claim requests, server-confirmed activity claim settlements, Gensoulkyo activity-claim response projection, idempotent `match_id:user_id` and `claim_kind:claim_id:user_id` settlement, duplicate compensation/claim rejection, and client-authored reward rejection implemented. The Matchmaking page now exposes a Result Preview action that consumes a server-shaped local settlement payload, grants points/dust/key/card and Boss chest preview rewards through the existing result service, opens the Results page, and shows score/reward/wallet/task/event rows as a playable UI loop from lobby/play/matchmaking into rewards. Real server RPC transport, batch leaderboard rewards, login claim confirmation, and polished result screens are pending. |
| Client tests | Started | Headless Godot smoke test for home gameplay tick staying paused with zero demo bullets, practice gameplay tick advancing only after entering practice, home surface visibility with gameplay/HUD hidden, adaptive portrait-panel sizing across 1280x720, 1920x1080, and 640x720 viewport checks, four-entry home primary button routing for Play/Collection/Community/Player Settings, hidden home dashboard/card surface, no-debug-wording guard for the player-facing home menu, second-level menu shell rendering, `ClientMenuPageModel` scene-contract loading and named-binding validation, runtime scene-backed host checks for Home Lobby, Menu Hub, Community Panel, Matchmaking Panel, Playfield Overlay, Collection Panel, and Settings Panel, `page_layout()` policy checks for home lobby, play/collection/community/settings hubs, matchmaking, network room, practice playfield, and running battle room, collection hub routing for Deck/Chest/Replay/Workshop, expanded second-level Activity/Friends/Promo/Deck status-card routing, social announcement CTA/friend presence/promotion-channel metadata, activity Social/Promotions navigation, Friends/Promotions cross-page navigation, input profile application/rebinding/restore/capture, gamepad curve rows/options/ranges/visible-preview/speed-preview/reset/reset-all, player-settings snapshot summary, player-facing page-experience summaries and `player_task_groups` for play/collection/matchmaking/community/settings/input pages, player-settings JSON save/load plus explicit save/reload/restore-default menu actions, player-settings focus/overview shortcuts for gamepad curve, key binding, volume, and display resolution, audio/display/accessibility settings mutation/reset/reset-all/persistence and control metadata, character rows/localized names/movement/shot/modifier behavior, bullet visual classification/readability/card-marker/high-density behavior, stage-select rows/recommended-character/stage-briefing/math-route/stage-run/all-pattern-emitter coverage, Boss pattern catalog executable requirement coverage for common Boss bullet families, deterministic emitter signatures, seed-variant checks, shaped laser/continuous-graze checks, behavior-spawn checks, Pattern Lab math-basis/density/danger/readability coverage and UI accept behavior, balance simulation aggregate/readability/performance rows, latency matrix aggregate/reconnect/mode-gate rows, deck builder filtering/validation/server-level projection/server-card-upgrade projection/UI edit/save/snapshot, local matchmaking/session/mode rows including PvP Duel metadata, Gensoulkyo HTTP contract projection, battle allocation/ticket/result-submit request and response projection, shared protocol descriptor loading/minimal contract validation, battle client prepare/connect/input-header scaffold actions, network-security business/battle transport/result-callback status rows, business envelope scaffold request headers including timestamp/status/replay-stale checks, authoritative server bullet/event-stream/active-card/mode-action/replay/rematch projection, mode/result/activity/chest authority boundaries, category-tab/status-card/page-focus/overview-card/quick-action coverage on second-level pages, play/matchmaking overview cards for Practice/Matchmaking/PvP Duel/World Boss/Room/Deck and Quick/Ranked/PvP/Boss/Room matching, prioritized quick actions for certification, collection, community, activity, friends, social, promotions, player settings, and input settings, UI accept actions for deck/settings/input/audio/display/mode/certification queue/battle-royale candidate/Boss transfer/activity/queue/network/chest/theme/stage/pattern/Pattern Lab/character/recommended-character/stage-run rows, replay save/load/index/seek/final hash validation, runtime theme registry, asset/theme manifests, i18n pack coverage, and live local Gensoulkyo HTTP integration paths implemented. |
| Asset manifest | Started | Machine-readable asset manifest, base theme manifest, replacement policy validation, and headless manifest checker implemented. A self-authored base-theme lobby standee SVG placeholder is now imported under `godot/themes/base/ui/` and recorded in `godot/assets/asset_manifest.json`; no third-party production assets are imported yet. |
| UI | Started | Home and all current second-level scene families are now runtime scene-backed instead of contract-only. `home_lobby_view.tscn`, `menu_hub_view.tscn`, `community_panel_view.tscn`, `settings_panel_view.tscn`, `matchmaking_panel_view.tscn`, `playfield_overlay_view.tscn`, and `collection_panel_view.tscn` are instantiated at runtime, named bindings are validated, and the existing row/action controls are mounted into the active scene host selected by `page_layout().scene_id`. Gameplay playfield/HUD are hidden on home, the local bullet-demo tick is paused while the client stays on home/menu pages, a responsive large character portrait/standee area is reserved, compact client status is shown, and the player-facing lobby now keeps four primary buttons: Play, Collection, Community, and Player Settings. Certification and matchmaking are grouped under Play; Deck, Chest, Replay, and Workshop are grouped under Collection; Activity, Friends, Social, and Promotions are grouped under Community; gamepad curve, key binding, volume, resolution, display, accessibility, storage, and reset flows are grouped under Player Settings. The old home dashboard/card surface is intentionally hidden so the first screen reads as a clean lobby instead of a debug launcher. `ClientMenuPageModel` owns both page specs and scene contracts for home, hub, community, settings, matchmaking, playfield, and collection scene families, while `UIScreenModel.page_layout()` formalizes category, parent route, primary rows, player task groups, panel anchor/width, gameplay draw, gameplay tick policy, required scene bindings, and render slots. Player settings/input/audio/display preserve the same gamepad movement curve, key binding, volume, resolution, display, accessibility, storage, reset, focus, overview-card, and quick-action row pipeline; player-settings snapshots now summarize gamepad/key/audio/display/storage state, and the gamepad curve preview includes actual move/focus speed samples for the selected character. Matchmaking and pre-fight network rooms are menu pages, practice is the local playfield, and running network battle pages can draw server-projected bullets without advancing local practice simulation. The home layout exposes testable viewport/panel/portrait/menu/dashboard/button rectangles and switches between portrait-first wide layout and compact button-first stacking for narrow widths. Second-level pages keep the richer shell with active-screen-windowed clickable navigation rail, Play/Collection/Community/Settings category tabs, localized navigation path, client status with settings/capture/network-security state, eight status cards for Play, Certification, Activity, Friends, Promo, Collection, Social, and Settings, page focus panel, player-facing page-experience summary, section summary/tabs, overview/action cards, page-spec-prioritized quick actions for certification, collection, community, activity, friends, social, promotions, player settings, and input settings, contextual selector/slider/toggle/capture/reset buttons, row buttons, and full row metadata for play/certification/practice/matching/network/modes/collection/deck/chest/activity/community/friends/social/promotions/Workshop/replay/player-settings/input/audio/display/advanced/results screens. Network Match exposes business HTTPS/WSS/ECC/sign targets, authenticated business-envelope scaffold status/replay guard, battle ECDHE/KCP/protobuf/ChaCha20 targets, and BattleNetworkClient prepare/connect/input-header actions as first-screen cards while clearly leaving real X25519/AEAD/sign and KCP/AEAD implementation pending. Page-specific final visual composition, production portrait assets, deeper mouse/focus styling, device-specific setting profiles, and production styling are pending. |

UI planning note: `ClientMenuPageModel` owns the client page map and scene contract map used by `UIScreenModel.page_layout()` and the runtime shell. It records each screen's layout kind, category, parent route, density, primary and secondary row ids, setting/social/mode groups, panel policy, gameplay draw/tick policy, scene id, required named bindings, and render slots. Runtime migration is now complete for the shared scene families: Home Lobby, Menu Hub, Community Panel, Settings Panel, Matchmaking Panel, Playfield Overlay, and Collection Panel are instantiated and smoke-tested as active hosts while the row/action model remains shared. Next UI migration should improve each scene's page-specific layout and production presentation instead of adding another parallel navigation implementation.

2026-06-28 UI/assets increment: Matchmaking now includes a Result Preview card for the local server-shaped settlement loop, so the client can run Play -> Matchmaking -> Results/Rewards without real transport. `client_ui_smoke_test.gd` covers the Result Preview action, result page routing, reward item rows, wallet summary, tasks, events, and reward audit. The base lobby portrait area now loads `res://themes/base/ui/lobby_standee.svg`, a self-authored original placeholder recorded in the asset manifest.

2026-06-28 UI/assets increment: `ClientMenuPageModel.page_spec()` now records per-page visual assets, visual treatment names, and state regions for home, Play, Collection, Community, Player Settings, matchmaking, network room, practice, collection detail, community detail, settings detail, and results pages. `main.gd` exposes those contracts in UI snapshots/page summaries, `client_ui_smoke_test.gd` asserts non-empty visual/state contracts, and `ci_static_checks.py` verifies that required pages keep the four-entry home hierarchy and that referenced UI placeholder art is registered in `asset_manifest.json`. Two self-authored original SVG placeholders, `mode_card_frame.svg` and `collection_card_frame.svg`, were added under the base theme with provenance records for future page-specific skinning.

2026-06-28 UI/assets increment: Home, Play, Collection, Community, and Player Settings now carry page-level layout-slot, status-region, controller-action, focus-action, standee/frame, and asset-usage metadata in `ClientMenuPageModel`. Runtime UI snapshots/page summaries expose those fields, and `client_ui_smoke_test.gd` now checks target-page layout/status/asset contracts plus Play and Collection focus-action routing. Asset records now include explicit machine-readable provenance, `tools/asset_manifest_check.gd`/`ci_static_checks.py` require it, and `godot/assets/licenses/README.md` lists the three self-authored base UI placeholders. Linux Godot was not available in this worker environment (`godot`/`godot4` not found), so only Python static checks were run here.

2026-06-29 UI/assets increment: `ClientMenuPageModel.page_spec()` now supplies default layout slots, controller actions, focus action ids, and asset-usage provenance for all page specs, so Play/Collection/Community/Player Settings detail pages inherit the same scene-family contracts as the primary hubs. The runtime UI shell now wires visible buttons into an explicit focus-neighbor chain and exposes headless health counters for focusability, focus neighbors, button target size, text clipping, and overlap. `client_ui_smoke_test.gd` fails on missing focus paths, tiny visible controls, unclipped button text, and missing secondary controller metadata, while `asset_manifest_check.gd` now validates boolean `modified` fields without GDScript constructor errors. Verified with `python3 tools/ci_static_checks.py`, Godot 4.7 headless `asset_manifest_check.gd`, and Godot 4.7 headless `client_ui_smoke_test.gd`.

2026-06-29 UI/assets increment: Home, Play, Collection, Community, and Player Settings page specs now explicitly record keyboard/gamepad/mouse support, focus-section groups, and button/label text-fit policy. The runtime UI snapshot exposes those fields, focus health now requires complete directional neighbors, `client_ui_smoke_test.gd` asserts the interaction/text-fit contract on the second-level hubs, and `ci_static_checks.py` rejects missing input-method, focus-section, or text-fit metadata before runtime. Verified with `python3 tools/ci_static_checks.py`, Godot 4.7 headless `asset_manifest_check.gd`, and Godot 4.7 headless `client_ui_smoke_test.gd`.

2026-06-29 UI/assets increment: Play, Collection, Community, Player Settings, and their key detail pages now declare concrete runtime focus sections for category tabs, page focus panels, overview cards, row windows, shell quick routes, and contextual control buttons. The runtime UI snapshot now reports visible coverage for declared focus sections plus mouse-operability health for every visible button, treating selector/reset control buttons as contextual instead of always-on. `client_ui_smoke_test.gd` fails if a visible page declares focus sections with no rendered controls or if a visible focusable button is not mouse-operable, while `ci_static_checks.py` guards the new runtime/smoke tokens. Verified with `python3 tools/ci_static_checks.py`, Godot 4.7 headless `asset_manifest_check.gd`, and Godot 4.7 headless `client_ui_smoke_test.gd`.

2026-06-29 bullet/replay increment: Boss spellbook previews now include compact deterministic signature digests and a golden fixture for the current four-phase local practice spellbook. `boss_pattern_catalog_check.gd` validates those golden previews, Pattern Lab preview budget headroom/status, replay-list spellbook preview metadata, and the local-preview `server_authoritative=false` boundary. This remains a practice/replay/display contract only; online Boss HP, damage, rewards, and settlement remain server-owned.

2026-06-29 bullet/replay increment: Golden Boss spellbook preview fixtures now cover every phase as an enforced contract: missing/orphan fixtures fail, and each fixture pins digest, sample count, max emit, cap, and budget headroom. Pattern Lab preview rows are checked against deterministic preview exports for digest/max emit/headroom/status parity, replay snapshot-to-index metadata now preserves preview budget fields for all phases, and the catalog gate rejects local spellbook preview replays that try to mark themselves server-authoritative. This is still local practice, deterministic preview, performance-budget, and replay-display validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Golden Boss spellbook fixtures now also pin the exact preview sample ticks and within-budget status, so fixture drift catches both signature changes and sample-window changes. Replay metadata validation rejects local Boss spellbook preview rows with negative budget headroom or non-`within_budget` status, in addition to rejecting missing preview ids/digests and any local preview marked server-authoritative. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`.

2026-06-29 bullet/replay increment: Replay index rows now preserve Boss spellbook preview sample ticks/count alongside preview export id, digest, budget headroom, and budget status. `ReplayStore.validate_spellbook_preview_metadata()` compares indexed local-practice rows against deterministic spellbook preview exports, and `boss_pattern_catalog_check.gd` rejects stale digest and stale sample-window fixtures so replay display metadata cannot drift from the golden preview contract. This remains local practice/replay display validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Pattern Lab Boss spellbook coverage now reads the canonical phase-script contract and carries preview sample tick/count metadata alongside digest, max emit, cap, headroom, and status. Generic replay index validation rejects missing or internally inconsistent spellbook preview sample windows before exact preview comparison, and `boss_pattern_catalog_check.gd` adds bad-count and missing-sample regression fixtures. This remains local practice, deterministic preview, performance-budget, and replay-display validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Replay list rows now surface specific Boss spellbook preview metadata failure states instead of collapsing every invalid local preview into `missing_spellbook_preview`: budget overruns report `preview_budget_overrun`, sample-window drift reports `bad_preview_sample_window`, and local practice rows marked server-authoritative report `local_preview_marked_authoritative`. `boss_pattern_catalog_check.gd` covers those row statuses so deterministic preview metadata remains a local practice/replay display contract and cannot imply server-owned Boss HP, damage, reward, or settlement authority.

2026-06-29 bullet/replay increment: Boss spellbook preview exports, Pattern Lab coverage rows, replay index rows, and replay-list rows now carry `preview_export_schema_version` plus compact per-sample signature digests. Golden fixtures pin the schema and sample digests, `ReplayStore` backfills the compact fields from existing full preview signatures for saved local-practice replays, and `boss_pattern_catalog_check.gd` rejects bad schema and stale sample-digest fixtures. This remains local practice, deterministic preview, performance-budget, and replay-display validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Generic replay-index validation now requires Boss spellbook preview sample ticks to match the canonical golden preview window, not merely any internally consistent tick array. The catalog gate adds a noncanonical sample-tick fixture and replay-row status assertion so stale local preview metadata is rejected before it can be shown as valid Replay display data. This remains local practice and deterministic preview validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook replay metadata now rejects negative per-sample preview digest values while preserving zero as the valid digest for empty deterministic sample ticks. `boss_pattern_catalog_check.gd` adds a negative sample-digest fixture and Replay row status assertion so corrupt compact preview arrays fail as `bad_preview_sample_window` before local practice Replay display. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook deterministic previews now carry and golden-pin per-sample emit counts alongside sample ticks and compact signature digests. Pattern Lab and Replay rows preserve those counts, legacy full-signature replay metadata can backfill them, and `boss_pattern_catalog_check.gd` rejects stale, negative, or mismatched emit-count arrays during exact preview validation. `BossPatternCatalog.validate_performance_budgets()` now honors each phase script's `bullet_cap_per_tick`, with a tight-cap regression fixture proving phase-local over-budget rows fail. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`; this remains a local practice/replay display contract only, while online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Replay index rows now retain legacy Boss spellbook full preview signatures when present and cross-check compact per-sample digest/emit-count arrays against that signature. `boss_pattern_catalog_check.gd` adds legacy-signature mismatch fixtures and Replay row status assertions, so old signature-backed local practice replays cannot be marked valid if compact preview metadata drifts. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Replay index rows now carry a compact Boss spellbook `preview_fixture_id` derived from spellbook id, phase id, and seed, and metadata validation rejects stale fixture/export ids with a specific `preview_fixture_mismatch` status before accepting local preview rows. `boss_pattern_catalog_check.gd` covers stale fixture and export-id regressions for both exact preview validation and Replay list status rows. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook Replay metadata now carries compact preview max-emit and bullet-cap fields in addition to per-sample emit counts and budget headroom. ReplayStore derives those fields for legacy signature-backed local previews, validates headroom as `cap - max_emit`, and rejects stale emit-count/max arithmetic before Replay rows can display as valid. This remains a local practice, deterministic preview, performance-budget, and Replay-display contract only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Generic Boss spellbook Replay index validation now cross-checks compact `preview_signature_digest` against the legacy full preview signature when present, returning `preview_signature_digest_mismatch` for stale digest rows before they can appear valid in Replay lists. `boss_pattern_catalog_check.gd` covers the stale digest at both index-validation and row-status levels. This remains local practice/replay display validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook deterministic previews, golden fixtures, Pattern Lab rows, Replay index rows, and Replay list rows now carry explicit preview sample-window bounds (`start`, `end`, `stride`) in addition to sample ticks, digests, emit counts, max emit, cap, and budget fields. Replay metadata validation backfills the bounds for legacy full-signature local previews and rejects stale start/end/stride rows as `bad_preview_sample_window`; this remains local practice/replay display validation only, with online Boss HP, damage, rewards, and settlement server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook preview exports, golden fixtures, Pattern Lab rows, Replay index rows, and Replay list rows now carry an explicit `preview_authority_scope=local_practice_preview_only` contract. Replay validation backfills the scope for legacy local-practice previews but rejects rows that claim any server-settlement authority with `preview_authority_scope_mismatch`, and the catalog gate covers the mismatch fixture. This remains deterministic local practice, performance-budget, and Replay-display validation only; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook deterministic preview exports now expose the compact `preview_fixture_id` directly, and Pattern Lab per-pattern spellbook rows carry the same preview export id, fixture id, digest, seed, max emit, bullet cap, headroom, and budget status as the phase coverage row. `boss_pattern_catalog_check.gd` now verifies every Pattern Lab spellbook pattern row against the deterministic preview export, so preview metadata cannot drift between phase coverage, pattern analysis, golden fixtures, and Replay display contracts. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`; this remains local practice/replay display validation only, while online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook golden preview fixtures now pin the compact preview export id and fixture id in addition to schema, authority scope, digests, sample windows, emit counts, max emit, cap, and headroom. `BossPatternCatalog.validate_spellbook_preview_exports()` and `boss_pattern_catalog_check.gd` reject stale golden fixture identity rows before Pattern Lab or Replay display can treat them as valid local-practice preview contracts. Verified with `python3 tools/ci_static_checks.py` and Godot 4.7 headless `boss_pattern_catalog_check.gd`; online Boss HP, damage, rewards, and settlement remain server-authoritative.

2026-06-29 bullet/replay increment: Boss spellbook Replay row metadata now distinguishes stale preview fixture ids from stale preview export ids: fixture drift still reports `preview_fixture_mismatch`, while export-id drift reports `preview_export_id_mismatch`. The catalog regression check and static guard cover the split so Replay UI can identify the exact local-practice preview contract field that drifted without implying any server-owned Boss HP, damage, reward, or settlement authority.
